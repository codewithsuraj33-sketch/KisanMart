import Link from 'next/link'
import { ArrowRight, BookOpen } from 'lucide-react'
import { POSTS } from '@/lib/blog-content'

export default function BlogPage() { return <main className="mx-auto w-full max-w-6xl flex-1 px-4 py-10 sm:px-6"><p className="text-xs font-bold uppercase tracking-wider text-brand">Knowledge center</p><h1 className="mt-2 font-display text-4xl font-extrabold text-ink">KisanMart field notes</h1><p className="mt-3 text-sm text-body">Clear, practical buying and crop-care guides.</p><div className="mt-8 grid gap-5 md:grid-cols-3">{POSTS.map((post) => <article key={post.slug} className="flex flex-col rounded-2xl border border-line bg-card p-6 shadow-card"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-sage text-brand"><BookOpen size={18} /></span><p className="mt-5 text-xs font-bold uppercase tracking-wider text-muted">{new Date(post.date).toLocaleDateString('en-IN', { day: 'numeric', month: 'short', year: 'numeric' })} · {post.read}</p><h2 className="mt-2 font-display text-xl font-bold leading-snug text-ink">{post.title}</h2><p className="mt-3 flex-1 text-sm leading-6 text-body">{post.excerpt}</p><Link href={`/blog/${post.slug}`} className="mt-5 flex items-center gap-1.5 text-sm font-bold text-brand">Read guide <ArrowRight size={15} /></Link></article>)}</div></main> }
